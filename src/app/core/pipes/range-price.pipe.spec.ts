import { RangePricePipe } from './range-price.pipe';

describe('RangePricePipe', () => {
  it('create an instance', () => {
    const pipe = new RangePricePipe();
    expect(pipe).toBeTruthy();
  });
});
